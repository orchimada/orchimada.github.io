{
  "name": "pin-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node test/parse.test.mjs && node test/view.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
